const schedule = require('node-schedule');
module.exports = (client) => {
    //const mcCH = client.channels.cache.get(process.env.POMF_ID)
    const mcAPI = process.env.MC_API
    const mcID = process.env.MC_ID
    let active = false;
    if(!active) return
    mcCH.send
        const repeat = schedule.scheduleJob('*/10 * * * *', function() {
        
        });

};